### Config for NSFF curl scene

expname = mvsnerf_llff

# Data
dataset_name = llff
datadir = /storage/datasets/real_iconic_noface
configdir = configs/ # Directory containing dataset splits
save_dir = /storage/projects/ganmvsnerf/runs

# Training
num_epochs = 6000
batch_size = 1024
# Encoding volume
use_mvs = True
pad = 24 # For MVS with multiple cameras, to be able to warp the volume
raw_noise_std = 1e0

# Network
use_viewdirs = True
pts_embedder = True
dir_embedder = True
netdepth = 8
netwidth = 256

# Losses
